{
  "name": "webloom",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "description": "Tree-based AI writing interface — HTMX + Bun, no build step",
  "scripts": {
    "dev": "bun run server.js",
    "start": "bun run server.js"
  }
}
